Paper A (cosmology / CLASS / P(k)) clean scripts

- These are clean copies for Paper A.
- They currently depend on the root-level `tcvphi` package (legacy path).
- Run from repo root, for example:
  - python papers/paper-01/code/test_tcv_background.py
  - python papers/paper-01/code/test_tcv_pk.py
