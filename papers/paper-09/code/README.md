# Paper IX code

This directory is reserved for paper-specific scripts used to freeze promoted figures and tables for Paper IX.

Rules:
- do not duplicate the full exploratory scan archive here,
- only promote the scripts that directly build figures/tables cited in `tex/paper-09.tex`,
- prefer thin wrappers around the canonical `papers/sm-exploration/` outputs.
